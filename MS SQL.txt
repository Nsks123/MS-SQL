--GROUP BY Function
select job from student
group by job;

select stuid,sum(stusalary) from student group by stuid;
select stuid,round(avg(stusalary),2) from student group by stuid;

--HAVING Clause
select stuid ,sum(stusalary) from student
group by stuid having stuid=2;
select user_id from users
group by user_id having user_id=1;

--TOP() Clause
 select top(2) user_id from users
 order by user_id desc;
select top(2) stusalary from student
 order by stuid desc;

 select * into emp_new1 from student
 select * from emp_new1;

 --AlTER
 alter table student add stufather varchar(20) ;
 --ALTER WITH CONSTRAINT
 alter table student add stuproject varchar(155) not null default 1;
--Alter with Reference
--alter table student add projectid integer  null
--constraint pID_unique_key unique;
--Using Alter to add many coloums
alter table student add projectid int, no_ofproject int null ;

--DROP
alter table student drop column projectid,no_ofproject;
alter table student drop constraint pID_unique_key;

alter table student alter column job integer;

create table employee(emp_id int,emp_name varchar(100));
alter table employee alter column emp_id varchar(20);
insert into employee values('20','sunil');
select * from employee as emp;
select emp_id as e_id from employee ;
select emp_id  e_id from employee ;*/