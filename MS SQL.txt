--while 
begin
while(select min(stusalary)from student)>10000
begin
update student set stusalary=stusalary+20000;
print 'salary updated';
if(select min(stusalary)from student)>=10000
print 'min.salary is greater or equal to 80000';
break;
end
end
go